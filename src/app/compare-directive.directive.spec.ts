import { CompareDirective } from './compare-directive.directive';

describe('CompareDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CompareDirective();
    expect(directive).toBeTruthy();
  });
});
